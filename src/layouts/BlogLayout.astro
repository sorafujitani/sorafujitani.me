---
interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description, image = '/profile.png' } = Astro.props;
const siteUrl = Astro.site || 'https://sorafujitani.me';
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
const ogImage = new URL(image, siteUrl);
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Sora Fujitani</title>
    {description && <meta name="description" content={description} />}
    <link rel="icon" type="image/png" href="/favicon.png" />

    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={`${title} | Sora Fujitani`} />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:image" content={ogImage} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={`${title} | Sora Fujitani`} />
    {description && <meta property="twitter:description" content={description} />}
    <meta property="twitter:image" content={ogImage} />
  </head>
  <body>
    <div class="container">
      <header>
        <a href="/" class="site-title">Sora Fujitani</a>
        <nav>
          <a href="/blog">Blog</a>
          <a href="/talks">Talks</a>
          <a href="/about">About</a>
        </nav>
      </header>
      <main>
        <slot />
      </main>
      <footer>
        <div class="footer-icons">
          <a href="https://x.com/_fs0414" target="_blank" rel="noopener noreferrer" aria-label="X (Twitter)">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
            </svg>
          </a>
          <a href="https://github.com/fs0414" target="_blank" rel="noopener noreferrer" aria-label="GitHub">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
          </a>
        </div>
        <p>&copy; 2025 Sora Fujitani</p>
      </footer>
    </div>
  </body>
</html>

<style>
  :global(:root) {
    /* Color Palette - Biome Theme */
    --color-bg: #181a1f;
    --color-text: #e1e4e8;
    --color-text-muted: #8b949e;
    --color-text-secondary: #c9d1d9;
    --color-heading: #ffffff;
    --color-border: #2d333b;
    --color-accent: #00d8ff;
    --color-accent-hover: #5ce6ff;

    /* Component Colors */
    --color-tag-bg: #21262d;
    --color-code-bg: #161b22;
    --color-code-text: #e1e4e8;

    /* Syntax Highlighting Colors */
    --syntax-keyword: #00d8ff;
    --syntax-function: #58a6ff;
    --syntax-string: #7ee787;
    --syntax-number: #79c0ff;
    --syntax-comment: #6e7681;
    --syntax-operator: #c9d1d9;
    --syntax-variable: #e1e4e8;
    --syntax-constant: #00d8ff;
  }

  :global(body) {
    margin: 0;
    padding: 0;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
    color: var(--color-text);
    background:
      radial-gradient(ellipse 80% 50% at 0% 0%, rgba(0, 136, 204, 0.15) 0%, transparent 50%),
      linear-gradient(180deg, #0d1117 0%, #161b22 100%);
    background-attachment: fixed;
    min-height: 100vh;
    line-height: 1.6;
  }

  .container {
    max-width: 680px;
    margin: 0 auto;
    padding: 0 24px;
  }

  header {
    padding: 48px 0 32px;
    border-bottom: 1px solid var(--color-border);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .site-title {
    font-size: 20px;
    font-weight: 600;
    color: var(--color-accent);
    text-decoration: none;
  }

  nav {
    display: flex;
    gap: 20px;
  }

  nav a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: 16px;
  }

  nav a:hover {
    color: var(--color-accent);
  }

  main {
    padding: 48px 0;
    min-height: 60vh;
  }

  footer {
    padding: 32px 0;
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  footer p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 14px;
  }

  .footer-icons {
    display: flex;
    justify-content: center;
    gap: 16px;
    margin-bottom: 16px;
  }

  .footer-icons a {
    color: var(--color-text-muted);
    transition: color 0.2s ease;
  }

  .footer-icons a:hover {
    color: var(--color-accent);
  }

  /* Footnotes */
  :global(.footnotes) {
    margin-top: 48px;
    padding-top: 24px;
    border-top: 1px solid var(--color-border);
    font-size: 14px;
    color: var(--color-text-muted);
  }

  :global(.footnotes h2) {
    display: none;
  }

  :global(.footnotes ol) {
    padding-left: 20px;
    margin: 0;
  }

  :global(.footnotes li) {
    margin-bottom: 8px;
  }

  :global(.footnotes li p) {
    margin: 0;
    display: inline;
  }

  :global(.footnotes a) {
    color: var(--color-accent);
    text-decoration: none;
  }

  :global(.footnotes a:hover) {
    color: var(--color-accent-hover);
  }

  :global(.data-footnote-backref) {
    margin-left: 4px;
    text-decoration: none;
  }

  :global(sup a[data-footnote-ref]) {
    color: var(--color-accent);
    text-decoration: none;
    font-size: 12px;
    font-weight: 600;
    padding: 0 2px;
  }

  :global(sup a[data-footnote-ref]:hover) {
    color: var(--color-accent-hover);
  }
</style>
