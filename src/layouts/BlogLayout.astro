---
import Search from '@components/Search.astro';
import StructuredData from '@components/StructuredData.astro';
import '@styles/global.css';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description, image = '/profile.png' } = Astro.props;
const isHomePage = Astro.url.pathname === '/';
import { SITE_URL, SITE_NAME } from '@lib/constants';

const siteUrl = Astro.site || SITE_URL;
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
const ogImage = new URL(image, siteUrl);
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | {SITE_NAME}</title>
    {description && <meta name="description" content={description} />}
    <link rel="icon" type="image/png" href="/favicon.png" />

    <!-- RSS Feed -->
    <link rel="alternate" type="application/rss+xml" title="Sora Fujitani Blog" href="/rss.xml" />

    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={`${title} | ${SITE_NAME}`} />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:image" content={ogImage} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={`${title} | ${SITE_NAME}`} />
    {description && <meta property="twitter:description" content={description} />}
    <meta property="twitter:image" content={ogImage} />

    <!-- Google Fonts: Fredoka -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@500;600&display=swap" rel="stylesheet" />

    <!-- Structured Data -->
    {isHomePage && <StructuredData type="WebSite" />}
  </head>
  <body>
    <div class="container">
      <header>
        <a href="/" class="site-title">
          <svg class="site-logo-cloud" width="22" height="15" viewBox="0 0 22 15" fill="none" xmlns="http://www.w3.org/2000/svg">
            <defs>
              <linearGradient id="cloud-grad" x1="0" y1="0" x2="22" y2="15" gradientUnits="userSpaceOnUse">
                <stop offset="0%" stop-color="#ffffff" />
                <stop offset="100%" stop-color="#b8e6ff" />
              </linearGradient>
            </defs>
            <path d="M17.5 13.5H5.5a4 4 0 0 1-.5-7.97A5.5 5.5 0 0 1 15.5 4a4.5 4.5 0 0 1 2 8.5z" fill="url(#cloud-grad)" stroke="#87CEEB" stroke-width="0.8"/>
          </svg>
          <span class="site-title-text">
            <span class="site-title-sora">sora</span>fujitani.me
          </span>
        </a>
        <button class="menu-toggle" aria-label="Menu"><span /></button>
        <div class="header-right">
          <nav>
            <a href="/blog">Blog</a>
            <a href="/talks">Talks</a>
            <a href="/pulls">Pulls</a>
            <a href="/about">About</a>
          </nav>
          <Search />
        </div>
      </header>
      <main>
        <slot />
      </main>
      <footer>
        <div class="footer-icons">
          <a href="https://x.com/sorafujitani" target="_blank" rel="noopener noreferrer" aria-label="X (Twitter)">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
            </svg>
          </a>
          <a href="https://github.com/sorafujitani" target="_blank" rel="noopener noreferrer" aria-label="GitHub">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
          </a>
          <a href="/rss.xml" aria-label="RSS Feed">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <path d="M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1Z"/>
            </svg>
          </a>
        </div>
        <p>&copy; {new Date().getFullYear()} {SITE_NAME}</p>
      </footer>
    </div>
  </body>
</html>

<script>
  const toggle = document.querySelector('.menu-toggle');
  const header = document.querySelector('header');
  if (toggle && header) {
    toggle.addEventListener('click', () => header.classList.toggle('open'));
    header.querySelectorAll('nav a').forEach((a) => {
      a.addEventListener('click', () => header.classList.remove('open'));
    });
  }
</script>

<style>
  .container {
    max-width: 680px;
    margin: 0 auto;
    padding: 0 24px;
  }

  header {
    padding: 48px 0 32px;
    border-bottom: 1px solid var(--color-border);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .site-title {
    display: flex;
    align-items: center;
    gap: 6px;
    font-family: 'Fredoka', sans-serif;
    font-size: 22px;
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.3s ease;
  }

  .site-title:hover {
    transform: translateY(-2px);
  }

  .site-title-text {
    background: linear-gradient(135deg, #87CEEB, #00d8ff, #5ce6ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none;
    filter: drop-shadow(0 1px 2px rgba(135, 206, 235, 0.3));
  }

  .site-title-sora {
    font-weight: 600;
    background: linear-gradient(135deg, #5ce6ff, #00d8ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .site-logo-cloud {
    transition: transform 0.3s ease, opacity 0.3s ease;
    filter: drop-shadow(0 1px 3px rgba(135, 206, 235, 0.4));
  }

  .site-title:hover .site-logo-cloud {
    animation: cloud-bounce 0.6s ease;
  }

  @keyframes cloud-bounce {
    0%, 100% { transform: translateY(0); opacity: 1; }
    30% { transform: translateY(-3px); opacity: 0.8; }
    60% { transform: translateY(-1px); opacity: 1; }
  }

  .header-right {
    display: flex;
    align-items: center;
    gap: 24px;
  }

  nav {
    display: flex;
    gap: 20px;
  }

  nav a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: 16px;
  }

  nav a:hover {
    color: var(--color-accent);
  }

  main {
    padding: 48px 0;
    min-height: 60vh;
  }

  footer {
    padding: 32px 0;
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  footer p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 14px;
  }

  .footer-icons {
    display: flex;
    justify-content: center;
    gap: 16px;
    margin-bottom: 16px;
  }

  .footer-icons a {
    color: var(--color-text-muted);
    transition: color 0.2s ease;
  }

  .footer-icons a:hover {
    color: var(--color-accent);
  }

  .menu-toggle {
    display: none;
  }

  @media (max-width: 768px) {
    header {
      flex-wrap: wrap;
    }

    .menu-toggle {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 32px;
      height: 32px;
      padding: 0;
      background: none;
      border: none;
      cursor: pointer;
    }

    .menu-toggle span,
    .menu-toggle span::before,
    .menu-toggle span::after {
      display: block;
      width: 18px;
      height: 2px;
      background: var(--color-text-muted);
      border-radius: 1px;
      transition: all 0.2s ease;
    }

    .menu-toggle span {
      position: relative;
    }

    .menu-toggle span::before,
    .menu-toggle span::after {
      content: '';
      position: absolute;
      left: 0;
    }

    .menu-toggle span::before {
      top: -6px;
    }

    .menu-toggle span::after {
      top: 6px;
    }

    header.open .menu-toggle span {
      background: transparent;
    }

    header.open .menu-toggle span::before {
      top: 0;
      transform: rotate(45deg);
    }

    header.open .menu-toggle span::after {
      top: 0;
      transform: rotate(-45deg);
    }

    .header-right {
      display: none;
      width: 100%;
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
      padding-top: 16px;
    }

    header.open .header-right {
      display: flex;
    }

    nav {
      flex-direction: column;
      gap: 0;
      width: 100%;
    }

    nav a {
      display: block;
      padding: 10px 0;
      font-size: 16px;
      border-bottom: 1px solid var(--color-border);
    }

    nav a:last-child {
      border-bottom: none;
    }
  }
</style>
